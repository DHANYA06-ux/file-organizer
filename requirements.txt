Flask>=2.0
pytest>=7.0

# Optional / recommended for full desktop experience
# Install these to enable the modern UI, plotting and TTS features used by
# `file_organiser_enhanced.py`.
customtkinter>=5.2.0
matplotlib>=3.7.0
pyttsx3>=2.90
schedule>=1.2.0
